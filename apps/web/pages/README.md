# Pages Layer (FSD Architecture)

This directory follows Feature-Sliced Design architecture.  
It's NOT Next.js Pages Router.

For routing, see `src/app/` directory.

이 디렉토리는 FSD 아키텍처를 따릅니다.  
이곳은 Next.js의 페이지 라우터가 아닙니다.

라우팅 관련 내용은 `src/app/` 디렉터리를 참조하세요.